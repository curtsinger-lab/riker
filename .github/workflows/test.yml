name: Run Tests

on:
  push:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on: [self-hosted, linux, x64]

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
    - name: make test
      run: make test