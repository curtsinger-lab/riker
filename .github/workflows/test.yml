name: Run Tests

on: push

jobs:
  build:
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - run: make
      - run: make test
